# EDQI 依赖
# 建议 Python 3.10–3.12；3.13 可用但部分 OCR/旧包需注意版本。

# ---- 核心（M1 引擎/可视化/ingest 必备）----
Pillow>=10.0.0
PyYAML>=6.0
jsonschema>=4.20.0
numpy>=1.24.0,<2.4
pymupdf>=1.23.0
pytest>=7.4.0
opencv-python-headless>=4.8.0

# ---- OCR 备选（Python 3.13 只能装到 1.2.x）----
# 不要写 >=1.3.0：1.3+ 要求 Python<3.12/3.13，在 3.13 上会直接失败
rapidocr-onnxruntime>=1.2.0,<1.3.0

# ---- 本地大模型（下载权重后再装；体积大）----
# torch>=2.1.0
# torchvision>=0.16.0   # Qwen3-VL AutoProcessor 需要
# transformers>=4.57.0
# accelerate>=0.33.0
# qwen-vl-utils>=0.0.14
# safetensors>=0.4.0
# huggingface_hub>=0.24.0
# modelscope>=1.14.0

# ---- 可选：新版 RapidOCR（包名已改为 rapidocr，需额外验证）----
# rapidocr>=3.0.0

# ---- 可选：PaddleOCR ----
# paddleocr>=2.7.0
